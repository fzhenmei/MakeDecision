@model MakeDecision.Web.Models.Cycle

@{
    ViewBag.Title = "明细";
}

<h2>明细</h2>

<fieldset>
    <legend>周期</legend>

    <div class="display-label">周期名称</div>
    <div class="display-field">@Model.CycleName</div>
</fieldset>
<p>
    @Html.ActionLink("编辑", "Edit", new { id=Model.Id }) |
    @Html.ActionLink("返回列表", "Index")
</p>


