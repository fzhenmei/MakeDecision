@model IEnumerable<MakeDecision.Web.Models.Cycle>

@{
    ViewBag.Title = "数据更新周期管理";
}

<h2>数据更新周期管理</h2>

<p>
    @Html.ActionLink("新建数据更新周期", "Create")
</p>
<table>
    <tr>
        <th>
            数据更新周期名称
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			@item.CycleName
        </td>
        <td>
            @Html.ActionLink("编辑", "Edit", new { id=item.Id }) |
            @Html.ActionLink("明细", "Details", new { id=item.Id }) |
            @Html.ActionLink("删除", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>


