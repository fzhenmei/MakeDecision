@model IEnumerable<MakeDecision.Web.Models.Department>

@{
    ViewBag.Title = "部门管理";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("新建部门", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            部门名称
        </th>
        <th>
            分类
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("修改", "Edit", new { id=item.Id }) |
            @Html.ActionLink("明细", "Details", new { id=item.Id }) |
            @Html.ActionLink("删除", "Delete", new { id=item.Id })
        </td>
        <td>
			@item.DepartmentName
        </td>
        <td>
			@(item.Categories == null ? "0" : item.Categories.Count.ToString())
            @Html.ActionLink("分类", "Create", "Category", new { departmentId = item.Id }, null)
        </td>
    </tr>
}

</table>


